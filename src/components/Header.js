import { Heading, Box } from '@chakra-ui/react';

const Header = () => (
  <Box mb={8} textAlign="center">
    <Heading as="h1" size="lg" my={4}>Hey, hoe kan ik je helpen?</Heading>
  </Box>
);

export default Header;